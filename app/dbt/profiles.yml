default:
  target: dev
  outputs:
    dev:
      type: sqlite
      threads: 1
      database: database
      schema: main
      schemas_and_paths:
        main: "/home/user/chargemaps/app/test.db"
      schema_directory: /home/user/chargemaps/app/dbt/models