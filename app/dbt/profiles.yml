default:
  target: dev
  outputs:
    dev:
      type: postgres
      host: localhost
      user: myuser
      password: my_password
      port: 5432
      dbname: licenses_db
      schema: public
      threads: 4
      schema_directory: /home/user/chargemaps/app/dbt/models